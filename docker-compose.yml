version: '3'
services:
  flask:
    build:
      context: ./flask
    ports:
      - "5000:5000"
  # mongodb:
  #   build:
  #     context: ./mongodb
  nginx:
    build:
      context: ./nginx
    volumes:
      - ./nginx/html:/usr/share/nginx/html
    ports:
      - "80:80"